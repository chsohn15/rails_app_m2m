
<% if @user.errors.any? %>
 id = "error_messages"
<% @user.errors.full_messages.each do |message|%>
    <li> <%= message %> </li>
    <%end %> 
    <% end %>

<%= form_for @user do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.label :balance %>
    <%= f.number_field :balance %> 
    <%= f.label :plants,"Plants:"%> 
    <%= f.collection_check_boxes :plant_ids, Plant.all, :id, :name %>
    <%=f.submit%>
<% end %>